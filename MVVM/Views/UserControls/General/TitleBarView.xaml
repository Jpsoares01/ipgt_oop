<UserControl x:Class="ipgt_oop.MVVM.Views.UserControls.General.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ipgt_oop.MVVM.Views.UserControls.General"
             mc:Ignorable="d">
    <Border Width="100">
        <Button Style="{StaticResource IconButton}"
                Command="{Binding CloseCommand}"
                HorizontalAlignment="Right"
                Margin="0,0,29,0">

            <Image x:Name="PasswordButtonImage"
                   Width="24"
                   Source="{StaticResource CloseIcon}"/>
        </Button>
    </Border>
</UserControl>
